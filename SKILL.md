---
name: eeg-preprocessing
description: 整理 EEG 数据集信息并生成报告；调研预处理方法并运行预处理；运行质量评估并生成报告。用于用户要求整理 EEG 数据集信息、运行预处理或进行质量评估时。
---
# EEG 数据集整理、预处理、质量评估及报告

## 调用与确认

1. 确定用户要求的处理类型，只选择一种：
   - 整理数据集信息，并生成报告
   - 调研预处理方法并运行预处理
   - 运行质量评估
2. 确定数据集名称或链接。
3. 确定是否调用 `$humanizer-zh`；默认不调用。
4. 读取对应调用路径的文件，并确定其中的确认项。
5. 仅询问缺失的信息，不重复询问用户已经明确的信息。
6. 开始处理前，用一条简短消息报告上述信息，请用户确认后继续。

## 调用路径

### 整理数据集信息

读取 [数据集信息](references/dataset-information.md)，调研数据集，生成数据集信息报告。

### 调研预处理方法并运行预处理

读取 [预处理](references/preprocessing.md)，调研预处理方法，运行预处理，生成预处理报告。

### 运行质量评估

读取 [质量评估](references/quality-assessment.md)，输出质量评估指标，运行可视化，生成质量评估报告。

## 输出边界

报告文件正文只包含用户或对应调用文件明确要求的内容。

报告类型、核查材料清单、调用路径、默认设置采用情况等未被明确要求写入报告的信息，只在生成报告时向用户说明，不写入正文或附录。

首次处理某一数据集时，在数据集文件夹下分别创建报告文件夹和预处理文件夹。全部报告及附件在报告文件夹中组织、保存；预处理产生的数据及其复现材料在预处理文件夹中组织、保存。

## Humanizer

若用户确认调用 `$humanizer-zh`，在报告完成后按照该 skill 的说明处理报告文本。
